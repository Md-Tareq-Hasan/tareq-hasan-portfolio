import React from 'react';
import Navbar from '../Navbar/Navbar';

const Resume = () => {
    return (
        <div>
            <Navbar></Navbar>
            <h1>hridoy</h1>
        </div>
    );
};

export default Resume;